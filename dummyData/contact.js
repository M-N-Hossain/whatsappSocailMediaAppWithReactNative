class Contact {
  constructor(id, name, imageUrl, chatHistory) {
    this.id = id;
    this.name = name;
    this.imageUrl = imageUrl;
    this.chatHistory = chatHistory;
  }
}

export default Contact;
